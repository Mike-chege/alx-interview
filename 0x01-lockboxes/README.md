## :file_folder: Lockboxes

This project covers a technical interview preparation(lockboxes), the main goal is to determine if all the boxes can be opened by returning `True`, else returning `False`.
